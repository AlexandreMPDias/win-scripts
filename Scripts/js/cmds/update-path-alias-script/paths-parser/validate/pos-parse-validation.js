const { makeAborter, makeValidate } = require('./helpers');
const { fn } = require('../../../../helpers');

/**
 * @param {{ source: import('../raw').Path.Obj }} paths
 */
function validateParsedContent(paths) {}

module.exports = { validateParsedContent };
